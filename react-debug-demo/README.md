# React Debug Demo

Small React + TypeScript + Vite project that compares buggy and fixed approaches for:

- form validation
- data fetching states
- state management and unnecessary re-renders

## Run

```bash
cd react-debug-demo
npm install
npm run dev
```

## Build

```bash
npm run build
```
